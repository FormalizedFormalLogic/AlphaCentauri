.lake

# Verso
_out

# VSCode
.vscode/settings.json
.vscode/mcp.json

# direnv
.envrc

# Claude-side working material stays out of git: reference PDFs (copyrighted), the draft
# roadmap, notes. Only the shared settings file is tracked.
.claude/*
!.claude/settings.json

# lefthook per-clone overrides (see lefthook.yml)
lefthook-local.yml
