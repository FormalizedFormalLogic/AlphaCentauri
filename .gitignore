.lake

# Verso
_out

# VSCode
.vscode/settings.json
.vscode/mcp.json

# direnv
.envrc

# Claude-side working material stays out of git: reference PDFs (copyrighted), plans, notes, and
# the per-clone operating notes in `.claude/AGENTS.md`. Only the shared settings file is tracked.
.claude/*
!.claude/settings.json
!.claude/commands/
!.claude/commands/**

# lefthook per-clone overrides (see lefthook.yml)
lefthook-local.yml
